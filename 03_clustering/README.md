# 03 · Clustering

`cluster.R` — auto-detects existing integration; runs Harmony on `sample` if none,
then PCA / neighbors / multi-resolution clustering / UMAP. PDF figures at each step
(elbow, resolution sweep, covariate UMAPs, cluster composition = integration check).
